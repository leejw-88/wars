oop =? 객체지향프로그래밍  코드의 재사용성과 중복제거가 가장 큰 목적
OOP
객체들이 서로 유기적으로 동작하는 프로그래밍 이론

추상화 =? 목적과 관련이 없는 부분을 제외해서 필요한 부분을 포착하는 기법
            (객체의 공통된 속성들 중 필요한 부분을 포착해서 클래스로 정의하는 설계 기법)
캡슐화 =? 외부에 노출할 필요가 없는 정보들은 은닉 (정보은닉)
상속 =? 상속 관계에 있는 두 클래스에 대해, 부모 클래스가 자손 클래스에게 속성을 물려주는 것
             (코드의 재사용이 목적)
다형성 =? 같은 형태이지만 다른 기능을 하는 것,오버라이딩은 이와 관련된 기능임

오버로딩(Overloading)은 기존에 없던 새로운 메서드를 정의하는 것이고, 
오버라이딩(Overriding)은 상속 받은 메서드의 내용만 변경

mvc =? MVC : Model, View, Controller의 약자 애플리케이션의 기능을 구분함으로, 정돈된 개발을 가능하게 한다.
Model =? 데이터 관련 로직 담당, DB와 소통(SELECT, INSERT, UPDATE, DELETE)
View =? 사용자가 보는 화면 담당(UI),일반적으로 HTML/CSS로 구성
Controller =? Model로 부터 데이터를 받음,데이터를 View로 전달함


Mybatis =? 자바 sql 매핑지원 DB언어기준으로 작성
SQL Mapper=? SQL 문으로 직접 디비를 조작한다.

JPA =? ORM기술에대한 표준 명세로 java에서 제공하는 api 
ORM =? (Object-Relation Mapping/객체-관계 매핑) 매서드쿼리로 DB의 쿼리를 처리가능

Lombok이란 어노테이션 기반으로 코드를 자동완성 해주는 라이브러리 입니다.

Spring Boot =? 스프링 프레임워크 기반 프로젝트를 복잡한 설정없이 쉽고 빠르게 만들어주는 라이브러리입니다.

GCP =? 클라우드

GIT - SVN =? 

예매-동기화 =? 멀티 쓰레드 상에서 하나의자원에 접근할때 - 싱크로나이즈드(동기화 처리)

ajax =? 자바스크립트를 이용해 서버와 브라우저가 비동기 방식으로 데이터를 교환할 수 있는 통신 기능
웹페이지의 속도향상
서버의 처리가 완료 될때까지 기다리지 않고 처리 가능하다.
서버에서 Data만 전송해면 되므로 전체적인 코딩의 양이 줄어든다.

xml =? 태그형태로 데이터를 전송 
json =? key : value 문자열을 전송
파싱 =? 데이터를 조립해 원하는 데이터를 분리하는것

open api =? 누구나 사용할수 있도록 공개된 api를 말합니다.
Open API의 의미는 인터넷 이용자가 일방적으로 웹 검색 결과 및 사용자인터페이스(UI) 등을 제공받는 데 그치지 않고 직접 응용 프로그램과 서비스를 개발할 수 있도록 공개된 API를 말합니다

api =? 어플리케이션 프로그래밍 인터페이스 로 응용프로그램에서 데이터를 주고받기위한 방법을의미 직접 DB에 접근하는게아니라 api를통해 주고받습니다.
ex> 손님 ->종업원(api) ->주문

장점 = 서비스 개발 시간을 단축 시킬수 있습니다.
단점 = 관리가 안될 경우 정보의 정확도가 떨어짐.

interface =? 기계(장치)끼리의 정보를 교환하기 위한 수단이나 방법을말합니다.

Spring Security는 Spring 기반의 애플리케이션의 보안(인증과 권한, 인가 등)을 담당하는 스프링 하위 프레임워크이다. 
웹 서비스에서 스프링 시큐리티는 MVC 패턴 이전에 '필터'로서 동작합니다.
필터란 클라이언트 요청이 서블릿으로 가기 전에 먼저 처리할 수 있도록 톰캣(WAS)에서 지원해주는 기능입니다.

